# reccs

## how to run

- venv, pip install...
- in another terminal:

```bash
# ? global?
npm install -D tailwindcss

# run!
npx tailwindcss -i ./static/src/input.css -o ./static/dist/css/output.css --watch
```
